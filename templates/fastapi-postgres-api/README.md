# {{APP_NAME}} — API

Generated by the **AI SaaS Factory**. An API-only FastAPI service backed by
PostgreSQL, with JWT auth and a sample `items` CRUD resource.

## Run locally

```bash
cp .env.example .env
docker compose up --build
```

- API + Swagger docs: http://localhost:8000/docs
- Health: http://localhost:8000/health

## Test

```bash
pip install -r requirements-dev.txt
pytest
```

See `ARCHITECTURE.md` and `contracts/openapi.yaml` for the frozen API contract.
