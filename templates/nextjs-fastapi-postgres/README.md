# {{APP_NAME}}

Generated by the **AI SaaS Factory**. This scaffold provides a Next.js frontend
(`web/`) + FastAPI backend (`api/`) + PostgreSQL, wired for local development via
Docker Compose.

## Run locally

```bash
cp .env.example .env
docker compose up --build
```

- Frontend: http://localhost:3000
- API + docs: http://localhost:8000/docs
- Health: http://localhost:8000/health

See `ARCHITECTURE.md` for the shared API and data contracts.
